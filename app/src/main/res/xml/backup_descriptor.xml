<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- TODO Remove the following "exclude" elements to make them a part of the auto backup -->
    <exclude
        domain="database"
        path="course_terms.db" />
    <!-- Exclude the shared preferences file that contains the GCM registrationId -->
    <exclude
        domain="sharedpref"
        path="alarmFile.xml" />
    <exclude
        domain="sharedpref"
        path="courseAlarms.xml" />
    <exclude
        domain="sharedpref"
        path="assessmentAlarms.xml" />
</full-backup-content>
